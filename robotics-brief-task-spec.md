# 机器人技术简报自动任务规范

## 目标

每天按 Asia/Shanghai 日期生成并发送一份完整中文《机器人 / SLAM / 控制 / AI Coding 技术深度简报》，并归档到本仓库 `master` 分支。

重点跟进：

1. 视觉 SLAM、激光 SLAM、LiDAR-inertial SLAM、多传感器融合定位建图、回环检测、地图压缩、动态环境鲁棒定位；
2. 机器人控制、运动规划、轨迹优化、MPC、强化学习控制、安全强化学习、sim-to-real、端到端控制；
3. vibe coding / AI coding agent 的突破性技术、框架、开源项目、工程工作流与安全治理；
4. 最新大模型、代码模型、多模态模型、机器人基础模型和端侧模型。

## 检索与时间窗口

- 必须先搜索并核验公开信息，优先原始论文、官方仓库、官方项目页、正式发布记录。
- 首先检查最近 24 小时；不足 5 条高质量且不重复的主动态时，依次扩展到最近 7 天、30 天、90 天，最后可回补约近 2 年仍有重要工程价值的工作。
- 不得凑数。主动态总数 5–8 条，SLAM 与机器人控制优先。
- 超出最近 24 小时的条目必须注明原始发布日期，并明确标为“时间回补”或“补充回顾”。
- 排序优先级：公开时间新近性 > 技术重要性 > 与 SLAM/控制相关性 > 可复现性 > 工程落地价值。

## 强制去重

选题前必须读取 `robotics-brief-covered-items.md`。

联合使用以下标识查重：规范化标题、arXiv ID、DOI、GitHub 仓库地址、项目主页。论文、项目页、代码仓库属于同一工作时视为同一条目。

索引中已存在的工作不得再次作为完整“新动态”。仅在出现新代码、权重、数据集、新版本、真实硬件结果或重大技术修订时可作为“更新”，并明确说明相对首次报道新增内容。历史导入待核验条目同样参与查重。

## 主动态写作要求

每条必须说明：

- 为什么重要；
- 适合谁关注；
- 工程落地启发；
- 原始论文、GitHub、官方文档或项目主页的可直接访问链接。

SLAM/控制条目还应尽可能提炼：算法模块、传感器或动力学假设、实时性、鲁棒性、可复现性、工程风险。

AI Coding/大模型条目重点判断：突破性工程价值、是否适合真实研发流程、权限/安全/可验证性风险。

## 经典论文回顾

主动态之后固定增加独立章节：

`## 经典论文回顾`

不计入 5–8 条主动态。每期 1 篇对当前 SLAM、定位建图、规划或控制仍有基础价值的经典论文。

必须说明：发表时间与历史位置、核心问题、算法模块或关键数学思想、传感器/动力学假设、当年为何重要、今天仍在使用的思想、已被后续替代的部分、公开代码/数据与可复现性、对当前工程项目的重新解读。

若索引中已经出现，只能标为“经典复盘”，并从明显不同的新角度分析。

## Markdown 结构

正文必须是可独立阅读的完整 Markdown，结构为：

- `# 机器人 / SLAM / 控制 / AI Coding 技术深度简报｜YYYY-MM-DD`
- `## 摘要`
- `## 1. ...` 至实际数量的连续编号主动态
- `## 经典论文回顾`
- `## 今日结论`
- `## 最值得深入研究或尝试复现的方向`
- `## 参考资料`

“为什么重要、算法模块、传感器假设、实时性、鲁棒性、可复现性、风险、适合谁关注、工程落地启发”等使用 `###`。

## 引用格式硬规则

GitHub/Jekyll 归档正文中禁止出现 ChatGPT 内部引用标记，包括但不限于：

- `citeturn...`
- `turn...`
- `filecite`
- 任何聊天内部 reference id

所有来源必须写成标准 Markdown 链接：`[名称](https://...)`。正文关键事实可在段尾直接附 `（[论文](URL)，[项目页](URL)）`。

提交前必须检查归档文本不存在上述内部引用残留。

## GitHub 文章归档

当天文章路径固定为：

`_posts/YYYY-MM-DD-robotics-brief.md`

日期使用 Asia/Shanghai。

front matter 必须为：

```yaml
---
layout: post
title: "机器人 / SLAM / 控制 / AI Coding 技术深度简报｜YYYY-MM-DD"
date: YYYY-MM-DD 09:00:00 +0800
description: "当日摘要的精简纯文本版本"
categories: [机器人技术简报]
tags: [SLAM, 机器人控制, AI-Coding, 大模型]
---
```

提交前先读取目标文件。不存在则创建；已存在则使用当前 blob SHA 更新，禁止重复日期文件。

commit message：

- 新建：`docs: add robotics brief for YYYY-MM-DD`
- 更新：`docs: update robotics brief for YYYY-MM-DD`

提交后必须重新读取文章，确认：日期正确、正文未截断、包含 `## 经典论文回顾` 和 `## 参考资料`、无聊天内部引用标记。

## 更新去重索引

文章归档并验证后，再次读取 `robotics-brief-covered-items.md` 获取当前 blob SHA。

- 追加当天首次覆盖的论文/项目；
- 对“更新”或“经典复盘”修改最近覆盖日期和备注；
- 更新文件头部最后更新日期和条目统计说明；
- commit message：`docs: update robotics brief coverage index for YYYY-MM-DD`。

提交后重新读取索引，确认当天条目真实存在、文件未截断、维护检查表仍在。

## 失败处理

任何检索、GitHub 读取、文章提交、文章验证、索引更新或索引验证失败，都不得静默结束。

- 能继续生成正文时，仍应发送完整正文；
- 最终明确说明失败的具体步骤和原因；
- 不得伪造 commit、文件、网页链接或声称归档成功。

## 最终输出

聊天中直接发送完整正文，不只给摘要或跳转链接。完成归档后附：

- `https://daily.helywin.com/reports/YYYY/MM/DD/`
- `https://github.com/helywin/daily/blob/master/_posts/YYYY-MM-DD-robotics-brief.md`
- `https://github.com/helywin/daily/blob/master/robotics-brief-covered-items.md`
